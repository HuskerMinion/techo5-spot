# Shell scripts and everything that lands on the device must stay LF whatever
# the checkout platform does; the rest follows the platform.
* text=auto
*.sh text eol=lf
*.py text eol=lf
tools/linux/** text eol=lf
*.ps1 text eol=crlf
*.png binary
*.tflite binary
*.wav binary
